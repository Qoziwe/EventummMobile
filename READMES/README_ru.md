# Eventum React Native Приложение

Это чистый UI-дизайн на React Native для приложения поиска мероприятий Eventum. **Без мок-данных и плейсхолдеров** — только готовые UI-компоненты, ожидающие ваши данные.

## Инструкция по установке

### 1. Создайте новый Expo-проект

```bash
npx create-expo-app eventum-mobile --template blank-typescript
cd eventum-mobile
```

### 2. Установите зависимости

```bash
npx expo install @react-navigation/native @react-navigation/native-stack @react-navigation/bottom-tabs react-native-screens react-native-safe-area-context @expo/vector-icons
```

### 3. Скопируйте файлы

Скопируйте все файлы из папки `react-native` в ваш Expo-проект:

- Скопируйте `App.tsx`, заменив стандартный `App.tsx`
- Создайте папку `theme/` и добавьте `colors.ts`
- Создайте папку `components/` и добавьте все файлы компонентов
- Создайте папку `screens/` и добавьте все файлы экранов

### 4. Запустите приложение

```bash
npx expo start
```

## Структура проекта

```
react-native/
├── App.tsx                     # Точка входа приложения с навигацией
├── theme/
│   └── colors.ts               # Цвета и дизайн-токены системы
├── components/
│   ├── Header.tsx              # Хедер приложения с поиском
│   ├── HeroSection.tsx         # Главный блок с фильтрами
│   ├── EventCard.tsx           # Переиспользуемая карточка события
│   ├── ForYouSection.tsx       # Карусель «Для вас»
│   ├── EventsGrid.tsx          # Карусель популярных событий
│   ├── CommunityPulse.tsx      # Блок сообществ
│   └── Footer.tsx              # Футер приложения
└── screens/
    ├── HomeScreen.tsx          # Главный экран
    ├── AllEventsScreen.tsx     # Все события с фильтрами
    ├── EventDetailScreen.tsx   # Детали события и покупка
    ├── ProfileScreen.tsx       # Профиль пользователя
    ├── CommunitiesScreen.tsx   # Список сообществ
    └── SearchScreen.tsx        # Экран поиска
```

## Использование — передача данных

Все компоненты принимают данные через props. Пример:

```tsx
// Использование EventCard
<EventCard
  title="Jazz Night"
  date="Сегодня, 20:00"
  venue="Blue Note Club"
  attendees={47}
  price="$35"
  category="Музыка"
  imageUri="https://your-image-url.com/image.jpg"
  onPress={() => navigation.navigate('EventDetail', { id: '1' })}
/>

// ForYouSection с дочерними элементами
<ForYouSection title="Для вас" subtitle="На основе ваших интересов">
  {events.map(event => <EventCard key={event.id} {...event} />)}
</ForYouSection>

// ProfileScreen с данными
<ProfileScreen
  avatarInitials="JD"
  name="John Doe"
  email="john@example.com"
  role="Исследователь"
  interests={[{ id: 'music', label: 'Музыка', icon: '🎵' }]}
  stats={[{ icon: 'ticket-outline', value: '12', label: 'Билеты' }]}
/>
```

## Что включено (чистый UI)

- Главный экран с секциями
- Экран всех событий с категориями
- Экран деталей события с блоком покупки
- Экран профиля со статистикой
- Экран сообществ с фильтрами
- Экран поиска с быстрыми фильтрами

## Что нужно добавить

- Интеграцию с API / backend
- Систему аутентификации
- Загрузку реальных данных (SWR, React Query и т.д.)
- Логику навигации
- Управление состоянием
- Push-уведомления
- Платёжную систему

## Кастомизация

Отредактируйте `theme/colors.ts`, чтобы настроить дизайн-систему под себя.
