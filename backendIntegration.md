# ПОЛНАЯ ИНСТРУКЦИЯ ПО ИНТЕГРАЦИИ REACT NATIVE С 1С:ПРЕДПРИЯТИЕ

## 📋 Содержание

1. [Обзор архитектуры](#обзор-архитектуры)
2. [Подготовка сервера и 1С](#подготовка-сервера-и-1с)
3. [Создание REST API в 1С](#создание-rest-api-в-1с)
4. [Настройка безопасности](#настройка-безопасности)
5. [Интеграция в React Native](#интеграция-в-react-native)
6. [Миграция данных](#миграция-данных)
7. [Тестирование](#тестирование)
8. [Развертывание](#развертывание)

---

## 🏗️ Обзор архитектуры

```
React Native App (EventummMobile)
         ↓ HTTPS
    REST API / OData
         ↓
    1С:Предприятие (Сервер)
         ↓
    База данных 1С (PostgreSQL/MS SQL)
```

**Технологии:**

- **1С:Предприятие 8.3** (конфигурация на базе БСП или собственная)
- **REST API** или **OData** для обмена данными
- **HTTPS** для безопасной передачи данных
- **JWT токены** для аутентификации

---

## 🖥️ Подготовка сервера и 1С

### Шаг 1: Установка 1С:Предприятие на сервере

1. **Требования к серверу:**
   - Windows Server 2016+ или Linux (Ubuntu 20.04+)
   - Минимум 4GB RAM, 2 CPU cores
   - 50GB свободного места на диске
   - Статический IP адрес или доменное имя

2. **Установка 1С:Сервер:**

   ```bash
   # Для Linux (Ubuntu/Debian)
   sudo dpkg -i 1c-enterprise83-server_*.deb
   sudo apt-get install -f

   # Для Windows
   # Запустить установщик 1c-enterprise83-server.exe
   ```

3. **Настройка кластера серверов:**
   - Запустить `ras` (Remote Administration Server)
   - Создать новый кластер
   - Настроить порты (по умолчанию: 1540, 1541)

### Шаг 2: Создание информационной базы

1. **Создание базы данных:**
   - Открыть консоль управления кластером
   - Создать новую информационную базу
   - Выбрать тип: **PostgreSQL** или **MS SQL Server**
   - Указать имя базы: `EventummDB`

2. **Настройка подключения:**
   ```
   Имя: EventummDB
   Тип: PostgreSQL
   Сервер БД: localhost
   Порт: 5432
   Имя БД: eventumm
   Пользователь: postgres
   Пароль: [ваш_пароль]
   ```

### Шаг 3: Установка конфигурации

1. **Вариант А: Создать собственную конфигурацию**
   - Создать новую конфигурацию в конфигураторе
   - Добавить необходимые объекты (см. ниже)

2. **Вариант Б: Использовать БСП (Библиотека стандартных подсистем)**
   - Установить БСП 3.1+
   - Использовать готовые механизмы REST API

---

## 🔧 Создание REST API в 1С

### Шаг 1: Создание структуры данных

#### Объекты конфигурации:

1. **Справочник "Пользователи"** (`Справочники.Пользователи`)

   ```
   - Код (строка)
   - Наименование (строка)
   - Email (строка, уникальный)
   - ПарольХеш (строка)
   - ТипПользователя (Перечисление: Исследователь, Организатор)
   - ДатаРождения (Дата)
   - Город (строка)
   - Интересы (ТаблицаЗначений)
   - ДатаРегистрации (Дата)
   - Активен (Булево)
   ```

2. **Справочник "События"** (`Справочники.События`)

   ```
   - Код (строка)
   - Наименование (строка)
   - Описание (строка, неограниченная)
   - Организатор (СправочникСсылка.Пользователи)
   - ДатаНачала (ДатаВремя)
   - ДатаОкончания (ДатаВремя)
   - Местоположение (строка)
   - Район (строка)
   - Цена (Число)
   - Категории (ТаблицаЗначений)
   - ВозрастноеОграничение (Число)
   - ИзображениеURL (строка)
   - СтатистикаПросмотров (Число)
   - ДатаСоздания (Дата)
   - Активно (Булево)
   ```

3. **Регистр сведений "Билеты"** (`РегистрыСведений.Билеты`)

   ```
   - Пользователь (СправочникСсылка.Пользователи)
   - Событие (СправочникСсылка.События)
   - Количество (Число)
   - ДатаПокупки (ДатаВремя)
   - IDБилета (строка, уникальный)
   ```

4. **Регистр сведений "Избранное"** (`РегистрыСведений.Избранное`)

   ```
   - Пользователь (СправочникСсылка.Пользователи)
   - Событие (СправочникСсылка.События)
   ```

5. **Регистр сведений "Подписки"** (`РегистрыСведений.Подписки`)

   ```
   - Исследователь (СправочникСсылка.Пользователи)
   - Организатор (СправочникСсылка.Пользователи)
   ```

6. **Документ "Обсуждения"** (`Документы.Обсуждения`)
   ```
   - Автор (СправочникСсылка.Пользователи)
   - Категория (строка)
   - Содержание (строка, неограниченная)
   - ВозрастноеОграничение (Число)
   - ДатаСоздания (ДатаВремя)
   - ГолосаЗа (Число)
   - ГолосаПротив (Число)
   ```

### Шаг 2: Создание HTTP-сервисов

#### Создать обработку "REST_API" (`Обработки.REST_API`)

**Модуль объекта:**

```bsl
#Область ПрограммныйИнтерфейс

// Основной метод обработки HTTP-запросов
Функция ОбработатьHTTPЗапрос(Запрос) Экспорт

	Попытка

		Путь = Запрос.Путь;
		Метод = ВерхнийРегистр(Запрос.Метод);

		// Разбор пути
		ЧастиПути = СтрРазделить(Путь, "/");

		// Удаляем пустые элементы
		Для Индекс = ЧастиПути.ВГраница() По 0 Цикл
			Если ПустаяСтрока(ЧастиПути[Индекс]) Тогда
				ЧастиПути.Удалить(Индекс);
			КонецЕсли;
		КонецЦикла;

		Если ЧастиПути.Количество() = 0 Тогда
			Возврат ОбработатьОшибку("Неверный путь запроса");
		КонецЕсли;

		Ресурс = ЧастиПути[0];

		// Маршрутизация
		Если Ресурс = "auth" Тогда
			Возврат ОбработатьАутентификацию(Запрос, Метод, ЧастиПути);
		ИначеЕсли Ресурс = "users" Тогда
			Возврат ОбработатьПользователей(Запрос, Метод, ЧастиПути);
		ИначеЕсли Ресурс = "events" Тогда
			Возврат ОбработатьСобытия(Запрос, Метод, ЧастиПути);
		ИначеЕсли Ресурс = "tickets" Тогда
			Возврат ОбработатьБилеты(Запрос, Метод, ЧастиПути);
		ИначеЕсли Ресурс = "discussions" Тогда
			Возврат ОбработатьОбсуждения(Запрос, Метод, ЧастиПути);
		Иначе
			Возврат ОбработатьОшибку("Неизвестный ресурс: " + Ресурс, 404);
		КонецЕсли;

	Исключение
		Возврат ОбработатьОшибку(ОписаниеОшибки());
	КонецПопытки;

КонецФункции

// Аутентификация
Функция ОбработатьАутентификацию(Запрос, Метод, ЧастиПути)

	Если Метод = "POST" И ЧастиПути.Количество() = 2 И ЧастиПути[1] = "login" Тогда
		Возврат ВходВСистему(Запрос);
	ИначеЕсли Метод = "POST" И ЧастиПути.Количество() = 2 И ЧастиПути[1] = "register" Тогда
		Возврат Регистрация(Запрос);
	ИначеЕсли Метод = "POST" И ЧастиПути.Количество() = 2 И ЧастиПути[1] = "refresh" Тогда
		Возврат ОбновитьТокен(Запрос);
	Иначе
		Возврат ОбработатьОшибку("Неверный метод или путь", 405);
	КонецЕсли;

КонецФункции

Функция ВходВСистему(Запрос)

	Тело = ПолучитьТелоЗапросаJSON(Запрос);

	Если Тело = Неопределено Тогда
		Возврат ОбработатьОшибку("Отсутствует тело запроса", 400);
	КонецЕсли;

	Email = Тело.email;
	Пароль = Тело.password;

	Если ПустаяСтрока(Email) Или ПустаяСтрока(Пароль) Тогда
		Возврат ОбработатьОшибку("Email и пароль обязательны", 400);
	КонецЕсли;

	// Поиск пользователя
	ЗапросПользователя = Новый Запрос;
	ЗапросПользователя.Текст =
		"ВЫБРАТЬ
		|	Пользователи.Ссылка КАК Ссылка,
		|	Пользователи.ПарольХеш КАК ПарольХеш,
		|	Пользователи.ТипПользователя КАК ТипПользователя
		|ИЗ
		|	Справочник.Пользователи КАК Пользователи
		|ГДЕ
		|	Пользователи.Email = &Email
		|	И Пользователи.Активен = ИСТИНА";

	ЗапросПользователя.УстановитьПараметр("Email", ВерхнийРегистр(Email));
	Результат = ЗапросПользователя.Выполнить();

	Если Результат.Пустой() Тогда
		Возврат ОбработатьОшибку("Неверный email или пароль", 401);
	КонецЕсли;

	Выборка = Результат.Выбрать();
	Выборка.Следующий();

	ПарольХеш = Выборка.ПарольХеш;

	// Проверка пароля (используем SHA-256 как в React Native)
	ПарольХешПроверка = ПолучитьХешСтроки(Пароль, "SHA256");

	Если ПарольХеш <> ПарольХешПроверка Тогда
		Возврат ОбработатьОшибку("Неверный email или пароль", 401);
	КонецЕсли;

	// Генерация JWT токена
	Токен = СоздатьJWTТокен(Выборка.Ссылка);

	// Получение данных пользователя
	ДанныеПользователя = ПолучитьДанныеПользователя(Выборка.Ссылка);

	Ответ = Новый Структура;
	Ответ.Вставить("success", Истина);
	Ответ.Вставить("token", Токен);
	Ответ.Вставить("user", ДанныеПользователя);

	Возврат СформироватьJSONОтвет(Ответ, 200);

КонецФункции

Функция Регистрация(Запрос)

	Тело = ПолучитьТелоЗапросаJSON(Запрос);

	Если Тело = Неопределено Тогда
		Возврат ОбработатьОшибку("Отсутствует тело запроса", 400);
	КонецЕсли;

	// Валидация
	Если ПустаяСтрока(Тело.email) Или ПустаяСтрока(Тело.password) Или ПустаяСтрока(Тело.name) Тогда
		Возврат ОбработатьОшибку("Email, пароль и имя обязательны", 400);
	КонецЕсли;

	// Проверка существующего email
	ЗапросПроверки = Новый Запрос;
	ЗапросПроверки.Текст =
		"ВЫБРАТЬ
		|	Пользователи.Ссылка
		|ИЗ
		|	Справочник.Пользователи КАК Пользователи
		|ГДЕ
		|	Пользователи.Email = &Email";

	ЗапросПроверки.УстановитьПараметр("Email", ВерхнийРегистр(Тело.email));
	РезультатПроверки = ЗапросПроверки.Выполнить();

	Если НЕ РезультатПроверки.Пустой() Тогда
		Возврат ОбработатьОшибку("Пользователь с таким email уже существует", 409);
	КонецЕсли;

	// Создание пользователя
	НовыйПользователь = Справочники.Пользователи.СоздатьЭлемент();
	НовыйПользователь.Наименование = Тело.name;
	НовыйПользователь.Email = ВерхнийРегистр(Тело.email);
	НовыйПользователь.ПарольХеш = ПолучитьХешСтроки(Тело.password, "SHA256");
	НовыйПользователь.ТипПользователя = ?(Тело.userType = "organizer", Перечисления.ТипыПользователей.Организатор, Перечисления.ТипыПользователей.Исследователь);
	НовыйПользователь.ДатаРождения = ?(ЕстьНеопределенноеЗначение(Тело.birthDate), '00010101', Тело.birthDate);
	НовыйПользователь.Город = ?(ЕстьНеопределенноеЗначение(Тело.location), "", Тело.location);
	НовыйПользователь.ДатаРегистрации = ТекущаяДата();
	НовыйПользователь.Активен = Истина;

	// Сохранение интересов
	Если ТипЗнч(Тело.interests) = Тип("Массив") Тогда
		Для Каждого Интерес Из Тело.interests Цикл
			НоваяСтрока = НовыйПользователь.Интересы.Добавить();
			НоваяСтрока.Значение = Интерес;
		КонецЦикла;
	КонецЕсли;

	НовыйПользователь.Записать();

	// Генерация токена
	Токен = СоздатьJWTТокен(НовыйПользователь.Ссылка);

	// Получение данных пользователя
	ДанныеПользователя = ПолучитьДанныеПользователя(НовыйПользователь.Ссылка);

	Ответ = Новый Структура;
	Ответ.Вставить("success", Истина);
	Ответ.Вставить("token", Токен);
	Ответ.Вставить("user", ДанныеПользователя);

	Возврат СформироватьJSONОтвет(Ответ, 201);

КонецФункции

// Обработка событий
Функция ОбработатьСобытия(Запрос, Метод, ЧастиПути)

	Токен = ПолучитьТокенИзЗаголовка(Запрос);
	Пользователь = ПроверитьТокен(Токен);

	Если Пользователь = Неопределено Тогда
		Возврат ОбработатьОшибку("Неавторизован", 401);
	КонецЕсли;

	Если Метод = "GET" Тогда
		Если ЧастиПути.Количество() = 1 Тогда
			// Список событий
			Возврат ПолучитьСписокСобытий(Запрос, Пользователь);
		ИначеЕсли ЧастиПути.Количество() = 2 Тогда
			// Одно событие
			Возврат ПолучитьСобытие(ЧастиПути[1], Пользователь);
		КонецЕсли;
	ИначеЕсли Метод = "POST" Тогда
		// Создание события
		Возврат СоздатьСобытие(Запрос, Пользователь);
	ИначеЕсли Метод = "PUT" И ЧастиПути.Количество() = 2 Тогда
		// Обновление события
		Возврат ОбновитьСобытие(Запрос, ЧастиПути[1], Пользователь);
	ИначеЕсли Метод = "DELETE" И ЧастиПути.Количество() = 2 Тогда
		// Удаление события
		Возврат УдалитьСобытие(ЧастиПути[1], Пользователь);
	КонецЕсли;

	Возврат ОбработатьОшибку("Неверный метод", 405);

КонецФункции

Функция ПолучитьСписокСобытий(Запрос, Пользователь)

	// Получение параметров запроса
	Параметры = Запрос.ПараметрыЗапроса;

	// Фильтры
	Категория = Параметры.Получить("category");
	Район = Параметры.Получить("district");
	Поиск = Параметры.Получить("search");
	Возраст = ПолучитьВозрастПользователя(Пользователь);

	ЗапросСобытий = Новый Запрос;
	ЗапросСобытий.Текст =
		"ВЫБРАТЬ
		|	События.Ссылка КАК Ссылка,
		|	События.Наименование КАК Наименование,
		|	События.Описание КАК Описание,
		|	События.Организатор КАК Организатор,
		|	События.Организатор.Наименование КАК ОрганизаторИмя,
		|	События.ДатаНачала КАК ДатаНачала,
		|	События.ДатаОкончания КАК ДатаОкончания,
		|	События.Местоположение КАК Местоположение,
		|	События.Район КАК Район,
		|	События.Цена КАК Цена,
		|	События.ВозрастноеОграничение КАК ВозрастноеОграничение,
		|	События.ИзображениеURL КАК ИзображениеURL,
		|	События.СтатистикаПросмотров КАК СтатистикаПросмотров
		|ИЗ
		|	Справочник.События КАК События
		|ГДЕ
		|	События.Активно = ИСТИНА
		|	И События.ВозрастноеОграничение <= &Возраст";

	ЗапросСобытий.УстановитьПараметр("Возраст", Возраст);

	Если НЕ ПустаяСтрока(Категория) Тогда
		ЗапросСобытий.Текст = ЗапросСобытий.Текст +
			" И СУЩЕСТВУЕТ(
			|	ВЫБРАТЬ
			|		1
			|	ИЗ
			|		События.Категории КАК Категории
			|	ГДЕ
			|		Категории.Значение = &Категория
			|)";
		ЗапросСобытий.УстановитьПараметр("Категория", НРег(Категория));
	КонецЕсли;

	Если НЕ ПустаяСтрока(Район) Тогда
		ЗапросСобытий.Текст = ЗапросСобытий.Текст + " И События.Район = &Район";
		ЗапросСобытий.УстановитьПараметр("Район", Район);
	КонецЕсли;

	Если НЕ ПустаяСтрока(Поиск) Тогда
		ЗапросСобытий.Текст = ЗапросСобытий.Текст +
			" И (События.Наименование ПОДОБНО &Поиск ИЛИ События.Описание ПОДОБНО &Поиск)";
		ЗапросСобытий.УстановитьПараметр("Поиск", "%" + Поиск + "%");
	КонецЕсли;

	ЗапросСобытий.Текст = ЗапросСобытий.Текст + " УПОРЯДОЧИТЬ ПО События.ДатаНачала";

	Результат = ЗапросСобытий.Выполнить();

	МассивСобытий = Новый Массив;
	Выборка = Результат.Выбрать();

	Пока Выборка.Следующий() Цикл
		Событие = Новый Структура;
		Событие.Вставить("id", Строка(Выборка.Ссылка));
		Событие.Вставить("title", Выборка.Наименование);
		Событие.Вставить("fullDescription", Выборка.Описание);
		Событие.Вставить("organizerId", Строка(Выборка.Организатор));
		Событие.Вставить("organizerName", Выборка.ОрганизаторИмя);
		Событие.Вставить("timestamp", Формат(Выборка.ДатаНачала, "ДФ=yyyy-MM-ddTHH:mm:ss"));
		Событие.Вставить("location", Выборка.Местоположение);
		Событие.Вставить("district", Выборка.Район);
		Событие.Вставить("priceValue", Выборка.Цена);
		Событие.Вставить("price", ?(Выборка.Цена = 0, "Бесплатно", Формат(Выборка.Цена, "ЧЦ=0; ЧВН=") + "₸"));
		Событие.Вставить("ageLimit", Выборка.ВозрастноеОграничение);
		Событие.Вставить("image", Выборка.ИзображениеURL);
		Событие.Вставить("stats", Выборка.СтатистикаПросмотров);

		МассивСобытий.Добавить(Событие);
	КонецЦикла;

	Ответ = Новый Структура;
	Ответ.Вставить("success", Истина);
	Ответ.Вставить("data", МассивСобытий);
	Ответ.Вставить("count", МассивСобытий.Количество());

	Возврат СформироватьJSONОтвет(Ответ, 200);

КонецФункции

Функция СоздатьСобытие(Запрос, Пользователь)

	// Проверка прав (только организаторы)
	Если Пользователь.ТипПользователя <> Перечисления.ТипыПользователей.Организатор Тогда
		Возврат ОбработатьОшибку("Только организаторы могут создавать события", 403);
	КонецЕсли;

	Тело = ПолучитьТелоЗапросаJSON(Запрос);

	Если Тело = Неопределено Тогда
		Возврат ОбработатьОшибку("Отсутствует тело запроса", 400);
	КонецЕсли;

	// Валидация
	Если ПустаяСтрока(Тело.title) Или ПустаяСтрока(Тело.description) Тогда
		Возврат ОбработатьОшибку("Название и описание обязательны", 400);
	КонецЕсли;

	НовоеСобытие = Справочники.События.СоздатьЭлемент();
	НовоеСобытие.Наименование = Тело.title;
	НовоеСобытие.Описание = Тело.fullDescription;
	НовоеСобытие.Организатор = Пользователь.Ссылка;
	НовоеСобытие.ДатаНачала = Тело.timestamp;
	НовоеСобытие.ДатаОкончания = ?(ЕстьНеопределенноеЗначение(Тело.endTimestamp), Тело.timestamp, Тело.endTimestamp);
	НовоеСобытие.Местоположение = Тело.location;
	НовоеСобытие.Район = Тело.district;
	НовоеСобытие.Цена = ?(ЕстьНеопределенноеЗначение(Тело.priceValue), 0, Тело.priceValue);
	НовоеСобытие.ВозрастноеОграничение = ?(ЕстьНеопределенноеЗначение(Тело.ageLimit), 0, Тело.ageLimit);
	НовоеСобытие.ИзображениеURL = ?(ЕстьНеопределенноеЗначение(Тело.image), "", Тело.image);
	НовоеСобытие.СтатистикаПросмотров = 0;
	НовоеСобытие.Активно = Истина;

	// Категории
	Если ТипЗнч(Тело.categories) = Тип("Массив") Тогда
		Для Каждого Категория Из Тело.categories Цикл
			НоваяСтрока = НовоеСобытие.Категории.Добавить();
			НоваяСтрока.Значение = НРег(Категория);
		КонецЦикла;
	КонецЕсли;

	НовоеСобытие.Записать();

	// Возврат созданного события
	ДанныеСобытия = ПолучитьДанныеСобытия(НовоеСобытие.Ссылка);

	Ответ = Новый Структура;
	Ответ.Вставить("success", Истина);
	Ответ.Вставить("data", ДанныеСобытия);

	Возврат СформироватьJSONОтвет(Ответ, 201);

КонецФункции

Функция ОбновитьСобытие(Запрос, IDСобытия, Пользователь)

	Попытка
		СсылкаСобытия = Справочники.События.НайтиПоКоду(IDСобытия);
	Исключение
		СсылкаСобытия = Справочники.События.ПолучитьСсылку(Новый УникальныйИдентификатор(IDСобытия));
	КонецПопытки;

	Если СсылкаСобытия.Пустая() Тогда
		Возврат ОбработатьОшибку("Событие не найдено", 404);
	КонецЕсли;

	Событие = СсылкаСобытия.ПолучитьОбъект();

	// Проверка прав (только организатор события)
	Если Событие.Организатор <> Пользователь.Ссылка Тогда
		Возврат ОбработатьОшибку("Нет прав на редактирование", 403);
	КонецЕсли;

	Тело = ПолучитьТелоЗапросаJSON(Запрос);

	// Обновление полей
	Если НЕ ЕстьНеопределенноеЗначение(Тело.title) Тогда
		Событие.Наименование = Тело.title;
	КонецЕсли;

	Если НЕ ЕстьНеопределенноеЗначение(Тело.fullDescription) Тогда
		Событие.Описание = Тело.fullDescription;
	КонецЕсли;

	// ... обновление остальных полей

	Событие.Записать();

	ДанныеСобытия = ПолучитьДанныеСобытия(СсылкаСобытия);

	Ответ = Новый Структура;
	Ответ.Вставить("success", Истина);
	Ответ.Вставить("data", ДанныеСобытия);

	Возврат СформироватьJSONОтвет(Ответ, 200);

КонецФункции

// Вспомогательные функции

Функция ПолучитьТелоЗапросаJSON(Запрос)

	Тело = Запрос.ПолучитьТелоКакСтроку();

	Если ПустаяСтрока(Тело) Тогда
		Возврат Неопределено;
	КонецЕсли;

	Попытка
		ЧтениеJSON = Новый ЧтениеJSON;
		ЧтениеJSON.УстановитьСтроку(Тело);
		Возврат ПрочитатьJSON(ЧтениеJSON);
	Исключение
		Возврат Неопределено;
	КонецПопытки;

КонецФункции

Функция СформироватьJSONОтвет(Данные, КодСтатуса = 200)

	ЗаписьJSON = Новый ЗаписьJSON;
	ЗаписьJSON.УстановитьСтроку();
	ЗаписатьJSON(ЗаписьJSON, Данные);

	Ответ = Новый HTTPСервисОтвет(КодСтатуса);
	Ответ.УстановитьТелоИзСтроки(ЗаписьJSON.Закрыть());
	Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
	Ответ.Заголовки.Вставить("Access-Control-Allow-Origin", "*");
	Ответ.Заголовки.Вставить("Access-Control-Allow-Methods", "GET, POST, PUT, DELETE, OPTIONS");
	Ответ.Заголовки.Вставить("Access-Control-Allow-Headers", "Content-Type, Authorization");

	Возврат Ответ;

КонецФункции

Функция ОбработатьОшибку(Сообщение, КодСтатуса = 400)

	Данные = Новый Структура;
	Данные.Вставить("success", Ложь);
	Данные.Вставить("error", Сообщение);

	Возврат СформироватьJSONОтвет(Данные, КодСтатуса);

КонецФункции

Функция ПолучитьТокенИзЗаголовка(Запрос)

	Авторизация = Запрос.Заголовки.Получить("Authorization");

	Если Авторизация = Неопределено Тогда
		Возврат "";
	КонецЕсли;

	// Формат: "Bearer <token>"
	Части = СтрРазделить(Авторизация, " ");

	Если Части.Количество() >= 2 Тогда
		Возврат Части[1];
	КонецЕсли;

	Возврат "";

КонецФункции

Функция ПроверитьТокен(Токен)

	Если ПустаяСтрока(Токен) Тогда
		Возврат Неопределено;
	КонецЕсли;

	// Простая проверка токена (в production использовать JWT библиотеку)
	// Здесь упрощенная версия - в реальности нужна полная валидация JWT

	Попытка
		// Декодирование токена и проверка
		// ... логика проверки JWT

		// Временная заглушка - возвращаем пользователя по токену
		// В production: декодировать JWT, проверить подпись, получить userId
		Возврат ПолучитьПользователяПоТокену(Токен);
	Исключение
		Возврат Неопределено;
	КонецПопытки;

КонецФункции

Функция СоздатьJWTТокен(СсылкаПользователя)

	// Упрощенная версия - в production использовать библиотеку JWT
	// Генерируем простой токен на основе UUID пользователя + timestamp + секретный ключ

	СекретныйКлюч = "YOUR_SECRET_KEY_HERE"; // Должен храниться в настройках
	Время = ТекущаяДата();
	ВремяИстечения = ДобавитьМесяц(Время, 1);

	Данные = Строка(СсылкаПользователя) + "|" + Формат(Время, "ДФ=yyyyMMddHHmmss") + "|" + Формат(ВремяИстечения, "ДФ=yyyyMMddHHmmss");
	Хеш = ПолучитьХешСтроки(Данные + СекретныйКлюч, "SHA256");

	Токен = Base64Строка(Данные + "|" + Хеш);

	// Сохранение токена в регистре для проверки
	МенеджерЗаписи = РегистрыСведений.ТокеныПользователей.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.Пользователь = СсылкаПользователя;
	МенеджерЗаписи.Токен = Токен;
	МенеджерЗаписи.ДатаСоздания = Время;
	МенеджерЗаписи.ДатаИстечения = ВремяИстечения;
	МенеджерЗаписи.Записать();

	Возврат Токен;

КонецФункции

#КонецОбласти
```

### Шаг 3: Настройка публикации HTTP-сервиса

1. **В консоли управления кластером:**
   - Открыть свойства информационной базы
   - Перейти на вкладку "HTTP-сервисы"
   - Добавить новый HTTP-сервис:
     ```
     Имя: REST_API
     Корневой URL: /api
     Обработка: REST_API
     ```

2. **Настройка виртуального каталога (IIS для Windows или Nginx для Linux):**

   **Для Windows (IIS):**
   - Установить модуль `httpPlatformHandler`
   - Создать виртуальный каталог, указывающий на `C:\Program Files\1cv8\srvinfo\reg_1541\`

   **Для Linux (Nginx):**

   ```nginx
   server {
       listen 80;
       server_name your-domain.com;

       location /api {
           proxy_pass http://localhost:1541/api;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
       }
   }
   ```

---

## 🔒 Настройка безопасности

### Шаг 1: Настройка HTTPS

1. **Получение SSL сертификата:**

   ```bash
   # Использовать Let's Encrypt (бесплатно)
   sudo certbot --nginx -d your-domain.com
   ```

2. **Настройка Nginx с HTTPS:**

   ```nginx
   server {
       listen 443 ssl;
       server_name your-domain.com;

       ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
       ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;

       location /api {
           proxy_pass http://localhost:1541/api;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
           proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
           proxy_set_header X-Forwarded-Proto $scheme;
       }
   }
   ```

### Шаг 2: Настройка CORS

В модуле обработки REST_API уже добавлены заголовки CORS. Для production настройте конкретные домены:

```bsl
Ответ.Заголовки.Вставить("Access-Control-Allow-Origin", "https://your-app-domain.com");
```

### Шаг 3: Настройка файрвола

```bash
# Разрешить только HTTPS
sudo ufw allow 443/tcp
sudo ufw allow 80/tcp
sudo ufw deny 1541/tcp  # Закрыть прямой доступ к 1С
```

---

## 📱 Интеграция в React Native

### Шаг 1: Установка зависимостей

```bash
npm install axios
npm install @react-native-async-storage/async-storage
npm install react-native-keychain  # Для безопасного хранения токенов
```

### Шаг 2: Создание API клиента

Создать файл `services/api.ts`:

```typescript
import axios, { AxiosInstance, AxiosError } from 'axios';
import * as Keychain from 'react-native-keychain';
import { Platform } from 'react-native';

// Базовый URL вашего сервера
const API_BASE_URL = __DEV__
  ? 'https://your-dev-server.com/api' // Для разработки
  : 'https://your-production-server.com/api'; // Для production

class ApiClient {
  private client: AxiosInstance;
  private token: string | null = null;

  constructor() {
    this.client = axios.create({
      baseURL: API_BASE_URL,
      timeout: 30000,
      headers: {
        'Content-Type': 'application/json',
      },
    });

    // Интерцептор для добавления токена
    this.client.interceptors.request.use(
      async config => {
        if (!this.token) {
          this.token = await this.getStoredToken();
        }

        if (this.token) {
          config.headers.Authorization = `Bearer ${this.token}`;
        }

        return config;
      },
      error => Promise.reject(error)
    );

    // Интерцептор для обработки ошибок
    this.client.interceptors.response.use(
      response => response,
      async (error: AxiosError) => {
        if (error.response?.status === 401) {
          // Токен истек или невалиден
          await this.clearToken();
          // Можно перенаправить на экран логина
        }
        return Promise.reject(error);
      }
    );
  }

  // Сохранение токена
  async saveToken(token: string): Promise<void> {
    this.token = token;
    await Keychain.setGenericPassword('api_token', token);
  }

  // Получение токена
  async getStoredToken(): Promise<string | null> {
    try {
      const credentials = await Keychain.getGenericPassword();
      if (credentials) {
        this.token = credentials.password;
        return credentials.password;
      }
    } catch (error) {
      console.error('Error getting token:', error);
    }
    return null;
  }

  // Очистка токена
  async clearToken(): Promise<void> {
    this.token = null;
    await Keychain.resetGenericPassword();
  }

  // Аутентификация
  async login(email: string, password: string) {
    const response = await this.client.post('/auth/login', {
      email,
      password,
    });

    if (response.data.success && response.data.token) {
      await this.saveToken(response.data.token);
      return response.data;
    }

    throw new Error(response.data.error || 'Ошибка входа');
  }

  async register(userData: {
    name: string;
    email: string;
    password: string;
    userType: 'explorer' | 'organizer';
    location: string;
    birthDate: string;
    interests?: string[];
  }) {
    const response = await this.client.post('/auth/register', userData);

    if (response.data.success && response.data.token) {
      await this.saveToken(response.data.token);
      return response.data;
    }

    throw new Error(response.data.error || 'Ошибка регистрации');
  }

  // События
  async getEvents(params?: {
    category?: string;
    district?: string;
    search?: string;
    limit?: number;
    offset?: number;
  }) {
    const response = await this.client.get('/events', { params });
    return response.data;
  }

  async getEvent(id: string) {
    const response = await this.client.get(`/events/${id}`);
    return response.data;
  }

  async createEvent(eventData: any) {
    const response = await this.client.post('/events', eventData);
    return response.data;
  }

  async updateEvent(id: string, eventData: any) {
    const response = await this.client.put(`/events/${id}`, eventData);
    return response.data;
  }

  async deleteEvent(id: string) {
    const response = await this.client.delete(`/events/${id}`);
    return response.data;
  }

  // Билеты
  async buyTicket(eventId: string, quantity: number) {
    const response = await this.client.post('/tickets', {
      eventId,
      quantity,
    });
    return response.data;
  }

  async getTickets() {
    const response = await this.client.get('/tickets');
    return response.data;
  }

  // Избранное
  async toggleFavorite(eventId: string) {
    const response = await this.client.post('/favorites', { eventId });
    return response.data;
  }

  async getFavorites() {
    const response = await this.client.get('/favorites');
    return response.data;
  }

  // Обсуждения
  async getDiscussions(params?: { category?: string; search?: string }) {
    const response = await this.client.get('/discussions', { params });
    return response.data;
  }

  async createDiscussion(discussionData: {
    categorySlug: string;
    content: string;
    ageLimit: number;
  }) {
    const response = await this.client.post('/discussions', discussionData);
    return response.data;
  }

  async addComment(postId: string, content: string) {
    const response = await this.client.post(`/discussions/${postId}/comments`, {
      content,
    });
    return response.data;
  }

  // Пользователи
  async getProfile() {
    const response = await this.client.get('/users/me');
    return response.data;
  }

  async updateProfile(profileData: any) {
    const response = await this.client.put('/users/me', profileData);
    return response.data;
  }

  async deleteAccount() {
    const response = await this.client.delete('/users/me');
    await this.clearToken();
    return response.data;
  }
}

export const apiClient = new ApiClient();
```

### Шаг 3: Обновление userStore для работы с API

Обновить `store/userStore.ts`:

```typescript
import { apiClient } from '../services/api';

// В функции register:
register: async (data) => {
  try {
    const response = await apiClient.register({
      name: data.name!,
      email: data.email!,
      password: data.password!,
      userType: data.userType || 'explorer',
      location: data.location || '',
      birthDate: data.birthDate || '',
      interests: data.interests || [],
    });

    set({
      user: response.user,
      isAuthenticated: true,
    });
  } catch (error: any) {
    throw new Error(error.message || 'Ошибка регистрации');
  }
},

// В функции login:
login: async (email, password) => {
  try {
    const response = await apiClient.login(email, password);

    set({
      user: response.user,
      isAuthenticated: true,
    });

    return true;
  } catch (error: any) {
    return false;
  }
},
```

### Шаг 4: Обновление eventStore

```typescript
import { apiClient } from '../services/api';

addEvent: async (event) => {
  try {
    const response = await apiClient.createEvent(event);
    set(state => ({
      events: [...state.events, response.data],
    }));
  } catch (error: any) {
    throw error;
  }
},

// Загрузка событий при старте
loadEvents: async () => {
  try {
    const response = await apiClient.getEvents();
    set({ events: response.data });
  } catch (error) {
    console.error('Error loading events:', error);
  }
},
```

### Шаг 5: Инициализация при запуске приложения

В `App.tsx`:

```typescript
import { useEffect } from 'react';
import { useEventStore } from './store/eventStore';
import { useUserStore } from './store/userStore';

export default function App() {
  const { loadEvents } = useEventStore();
  const { getStoredToken } = useUserStore();

  useEffect(() => {
    // Проверка сохраненного токена при запуске
    const init = async () => {
      const token = await apiClient.getStoredToken();
      if (token) {
        // Проверка валидности токена через запрос профиля
        try {
          const profile = await apiClient.getProfile();
          // Токен валиден, пользователь авторизован
        } catch {
          // Токен невалиден, очищаем
          await apiClient.clearToken();
        }
      }

      // Загрузка событий
      await loadEvents();
    };

    init();
  }, []);

  // ... остальной код
}
```

---

## 🔄 Миграция данных

### Шаг 1: Экспорт данных из локального хранилища

Создать скрипт миграции `scripts/migrateTo1C.ts`:

```typescript
import AsyncStorage from '@react-native-async-storage/async-storage';
import { apiClient } from '../services/api';

async function migrateData() {
  try {
    // Получение данных из локального хранилища
    const userData = await AsyncStorage.getItem('user-app-storage');
    const eventsData = await AsyncStorage.getItem('event-app-storage');

    if (userData) {
      const parsed = JSON.parse(userData);
      const user = parsed.state?.user;

      if (user && user.email) {
        // Попытка входа
        await apiClient.login(user.email, user.password);

        // Обновление профиля
        await apiClient.updateProfile({
          name: user.name,
          location: user.location,
          birthDate: user.birthDate,
          interests: user.interests,
        });
      }
    }

    if (eventsData) {
      const parsed = JSON.parse(eventsData);
      const events = parsed.state?.events || [];

      // Загрузка событий на сервер (только для организатора)
      for (const event of events) {
        if (event.organizerId === user?.id) {
          try {
            await apiClient.createEvent(event);
          } catch (error) {
            console.error('Error migrating event:', event.id, error);
          }
        }
      }
    }

    console.log('Migration completed');
  } catch (error) {
    console.error('Migration error:', error);
  }
}
```

---

## 🧪 Тестирование

### Шаг 1: Тестирование API

Использовать Postman или curl:

```bash
# Тест регистрации
curl -X POST https://your-server.com/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test User",
    "email": "test@example.com",
    "password": "Test1234",
    "userType": "explorer",
    "location": "Алматы",
    "birthDate": "2000-01-01"
  }'

# Тест входа
curl -X POST https://your-server.com/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "Test1234"
  }'

# Тест получения событий
curl -X GET https://your-server.com/api/events \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### Шаг 2: Тестирование в приложении

1. Проверить регистрацию и вход
2. Проверить загрузку событий
3. Проверить создание/редактирование событий
4. Проверить покупку билетов
5. Проверить работу офлайн (кэширование)

---

## 🚀 Развертывание

### Шаг 1: Настройка production сервера

1. **Обновить API_BASE_URL** в `services/api.ts`
2. **Настроить мониторинг** (логирование, метрики)
3. **Настроить резервное копирование** базы данных 1С
4. **Настроить автоматические обновления** безопасности

### Шаг 2: Обновление приложения

1. **Собрать production версию:**

   ```bash
   # Android
   npx expo build:android --type apk

   # iOS
   npx expo build:ios
   ```

2. **Обновить версию** в `app.json`

3. **Опубликовать** в Google Pla
